import crypto from "crypto"; export function canCreateEmergency(shadow:{state:string;riskScore:number}){return shadow.state==="DETERIORATING"&&shadow.riskScore>=70;} export function packetToken(){return crypto.randomBytes(24).toString("base64url");} export function tokenHash(x:string){return crypto.createHash("sha256").update(x).digest("hex");} export const ackDeadlineMs=5*60_000;
export function canTransitionEmergency(from:string,to:string){return (from==="OPEN"&&["ACKNOWLEDGED","ESCALATED","RESOLVED"].includes(to))||(from==="ACKNOWLEDGED"&&to==="RESOLVED")||(from==="ESCALATED"&&to==="RESOLVED");}
export function packetIsValid(incident:{packetExpiresAt?:Date;packetRevokedAt?:Date},token:string,hash?:string){return !!token&&!!hash&&!incident.packetRevokedAt&&!!incident.packetExpiresAt&&incident.packetExpiresAt.getTime()>Date.now()&&crypto.timingSafeEqual(Buffer.from(hash),Buffer.from(tokenHash(token)));}
