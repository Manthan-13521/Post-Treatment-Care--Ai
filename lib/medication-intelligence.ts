export type ApprovedMedicine={_id?:unknown;name:string;dose:string;schedule:string[]};
export function scheduleForDay(schedule:string[],day=new Date()){return schedule.map(time=>{const [hours,minutes]=time.split(":").map(Number);const at=new Date(day);at.setHours(hours,minutes,0,0);return at;});}
export function doseState(at:Date,now=new Date()){const delta=now.getTime()-at.getTime();if(delta>=0&&delta<=2*60*60_000)return "DUE_NOW" as const;if(delta>2*60*60_000)return "MISSED" as const;return "UPCOMING" as const;}
export function normalizeMedicine(value:string){return value.toLowerCase().replace(/[^a-z0-9]/g,"");}
export function compareApprovedMedicine(candidate:string,confidence:number,approved:ApprovedMedicine[]){if(confidence<0.9||!candidate.trim())return {result:"LOW_CONFIDENCE" as const,message:"I could not read the label confidently. Please check the package against your approved plan or ask your pharmacist/care team."};const match=approved.find(x=>normalizeMedicine(x.name)===normalizeMedicine(candidate));if(!match)return {result:"UNLISTED" as const,message:"This medicine is not on the doctor-approved medication plan. Do not start, substitute, change, or stop medicine based on this scan; contact your care team or pharmacist."};return {result:"MATCH" as const,medicationId:String(match._id),message:`This label matches your recorded doctor-approved medicine: ${match.name}. Follow the recorded schedule only; this does not change your prescription.`};}
