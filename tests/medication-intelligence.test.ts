import { describe,expect,it } from "vitest";
import { compareApprovedMedicine,doseState,scheduleForDay } from "@/lib/medication-intelligence";
describe("medication intelligence safety",()=>{const approved=[{_id:"m1",name:"Metformin",dose:"500 mg",schedule:["08:00"]}];it("matches only a high-confidence approved medicine",()=>{const result=compareApprovedMedicine("metformin",0.95,approved);expect(result.result).toBe("MATCH");expect(result.message).toContain("recorded doctor-approved");});it("refuses low-confidence label interpretation",()=>{const result=compareApprovedMedicine("metformin",0.5,approved);expect(result.result).toBe("LOW_CONFIDENCE");expect(result.message).toContain("could not read");});it("warns for a high-confidence unlisted medicine",()=>{const result=compareApprovedMedicine("amoxicillin",0.99,approved);expect(result.result).toBe("UNLISTED");expect(result.message).toContain("Do not start");});it("marks only recent doses as due now",()=>{const at=scheduleForDay(["08:00"],new Date("2026-01-01T08:30:00"))[0];expect(doseState(at,new Date("2026-01-01T08:30:00"))).toBe("DUE_NOW");expect(doseState(at,new Date("2026-01-01T11:00:00"))).toBe("MISSED");});});
