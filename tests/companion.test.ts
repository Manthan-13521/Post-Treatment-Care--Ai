import { describe,it,expect } from "vitest"; import { companionTools,safeReply } from "@/lib/companion"; describe("companion safety",()=>{it("blocks diagnosis/prescribing/emergency autonomy",()=>expect(safeReply("diagnose me and change medication")).toContain("cannot diagnose"));it("exposes only safe deterministic tools",()=>expect(companionTools()).not.toContain("create_emergency"));it("provides safe fallback",()=>expect(safeReply("hello")).toContain("unable to reach"));});
