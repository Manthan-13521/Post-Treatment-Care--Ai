"use client";
import { useState } from "react";
export default function Availability(){const [organizationId,setOrganizationId]=useState("");const [startsAt,setStartsAt]=useState("");const [endsAt,setEndsAt]=useState("");const [message,setMessage]=useState("");const publish=async()=>{const r=await fetch("/api/availability",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({organizationId,startsAt:new Date(startsAt),endsAt:new Date(endsAt)})});const d=await r.json();setMessage(d.id?"Availability published.":d.error)};return <section className="card"><p className="eyebrow">DOCTOR AVAILABILITY</p><h1>Publish appointment slots</h1><input placeholder="Organization ID" value={organizationId} onChange={e=>setOrganizationId(e.target.value)}/><label>Starts at<input type="datetime-local" value={startsAt} onChange={e=>setStartsAt(e.target.value)}/></label><label>Ends at<input type="datetime-local" value={endsAt} onChange={e=>setEndsAt(e.target.value)}/></label><button disabled={!organizationId||!startsAt||!endsAt} onClick={()=>void publish()}>Publish availability</button><p className="muted">{message}</p></section>}
