import Link from "next/link";
const steps=[['1','Start with recovery','A patient reviews monitored recovery and their approved medication plan.','/patient/medications'],['2','Prove medication safety','A label scan only checks for a high-confidence match against the doctor-approved plan; it never directs treatment.','/patient/medications'],['3','Book intentionally','CareShield searches persisted doctor availability, then requires patient confirmation for the selected slot.','/patient/appointments'],['4','Protect AI interaction','The companion and clinical copilot use a scanner and fixed tool firewall; the Security Command Center shows hash-only replay metadata.','/security'],['5','Coordinate deterioration','The War Room presents the deterministic emergency timeline, notification state, and ambulance-provider status.','/war-room']];
export default function Demo(){return <section className="card hero"><p className="eyebrow">Judge-ready walkthrough</p><h1>The CareShield story in five safe moves.</h1><p className="muted">Start with the no-sign-up patient workspace, which uses fictional embedded data. Protected links below are for authenticated, database-backed flows and safely fail without credentials.</p><Link className="button" href="/demo/patient">Start no-sign-up patient demo</Link><Link className="button" style={{marginLeft:8,background:"#102a32"}} href="/demo/video-care">Try video + AI demo</Link><div className="grid">{steps.map(([number,title,description,href])=><Link key={number} className="tile" href={href}><span className="tag">Step {number}</span><h2>{title}</h2><p className="muted">{description}</p><strong>Open protected flow →</strong></Link>)}</div><p className="muted">For a local demo dataset, use the guarded seed command with `CARESHIELD_SEED_DEMO=true`; it never runs implicitly.</p></section>}
