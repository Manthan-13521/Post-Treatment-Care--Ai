"use client";
import { useState } from "react";
export default function Onboarding(){const [message,setMessage]=useState("");async function submit(form:FormData){const r=await fetch("/api/onboarding",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:form.get("name"),role:form.get("role")})});setMessage(r.ok?"Profile saved.":"Please check the entered details.");}return <section className="card"><p className="eyebrow">PROFILE SETUP</p><h1>Complete your profile</h1><form action={submit}><label>Name<input name="name" minLength={2} required /></label><label>Role<select name="role" defaultValue="PATIENT"><option>PATIENT</option><option>GUARDIAN</option><option>DOCTOR</option><option>HOSPITAL_ADMIN</option></select></label><button>Save profile</button></form>{message&&<p>{message}</p>}</section>}
